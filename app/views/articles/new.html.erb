<h1>Create a new article</h1>

<%= form_with scope: :article, url: articles_path, local: true do |f| %>

    <p>
        <%= f.label :title %>
        </br>
        <%= f.text_field :title %>
    </p>
    <p>
        <%= f.label :description %>
        </br>
        <%= f.text_area :description %>
    </p>
    <p>
        <%= f.submit %>
    </p>

<% end %>





<p>This form is submitted to the new route, which is created by the create route. The create route has not view associated with it.</p>